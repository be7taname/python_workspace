'''
Created on Oct 3, 2015

@author: mianmianba
'''

class Multiply(object):
    def solution(self, num1, num2):
        """
        :type num1: str
        :type num2: str
        :rtype: str
        """
        num1 = num1[::-1]
        num2 = num2[::-1]
        prod = [0 for x in range(len(num1) + len(num2))]
        for a in num1:
            for b in num2:
        # return str(int(num1)*int(num2))