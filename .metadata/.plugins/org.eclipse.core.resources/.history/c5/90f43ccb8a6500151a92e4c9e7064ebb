'''
Created on Sep 27, 2015

@author: mianmianba
'''

class MaximalSquare(object):
    def solution(self, matrix):
        """
        :type matrix: List[List[str]]
        :rtype: int
        """
        numRow = len(matrix)
        numCol = len(matrix[0])
        sqrSize = [[0 for x in numCol] for y in numRow]
        for i in range(numRow):
            for j in range(numCol):