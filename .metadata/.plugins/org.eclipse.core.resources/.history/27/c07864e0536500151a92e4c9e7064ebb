'''
Created on Sep 27, 2015

@author: mianmianba
'''

class MaxProduct(object):
    def solution(self, nums):
        """
        :type nums: List[int]
        :rtype: int
        """
        numsLen = len(nums)
        if numsLen == 1: return nums[0]
        elif numsLen == 2:
            if ((nums[0] == 0 and nums[1] < 0) or (nums[0] == 0 and nums[1] < 0) or (nums[0] == 0 and nums[1] < 0)):        