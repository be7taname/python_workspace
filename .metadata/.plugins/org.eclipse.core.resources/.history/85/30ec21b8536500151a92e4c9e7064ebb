'''
Created on Sep 27, 2015

@author: mianmianba
'''

class MaxProduct(object):
    def solution(self, nums):
        """
        :type nums: List[int]
        :rtype: int
        """
        numsLen = len(nums)
        if numsLen == 1: return nums[0]
        elif numsLen == 2:
            n0 = nums[0]
            n1 = nums[1]
            if ((nums))        